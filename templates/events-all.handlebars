
{{#if userCtx.name}}
<a class="btn" href="#/events/new">New Event</a>
{{/if}}

<table class="table striped-table">
  <thead>
    <tr>
      <th>Name</th>
      <th>Date</th>

    </tr>
  </thead>
  <tbody>
{{#each rows}}
    <tr>
      <td><a href="#/event/{{id}}">{{name}}</a></td>
      <td><a href="#/event/{{id}}">{{date_formated}}</a></td>

    </tr>
{{/each}}
  </tbody>
</table>
<ul>

